import{_ as e,o as a,c as o,Q as i}from"./chunks/framework.7cd3ab18.js";const y=JSON.parse('{"title":"Using JuleC (Reference Compiler)","description":"","frontmatter":{},"headers":[],"relativePath":"compiler/compiling/using-julec.md","filePath":"compiler/compiling/using-julec.md"}'),t={name:"compiler/compiling/using-julec.md"},s=i('<h1 id="using-julec-reference-compiler" tabindex="-1">Using JuleC (Reference Compiler) <a class="header-anchor" href="#using-julec-reference-compiler" aria-label="Permalink to &quot;Using JuleC (Reference Compiler)&quot;">​</a></h1><p>Let&#39;s start with a simple hello world program and learn compiling from this program.</p><p>We have a <code>main.jule</code> file:</p><div class="language-jule line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">jule</span><pre class="shiki github-dark"><code><span class="line"><span style="color:#F97583;">fn</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">main</span><span style="color:#E1E4E8;">() {</span></span>\n<span class="line"><span style="color:#E1E4E8;">    </span><span style="color:#79B8FF;">outln</span><span style="color:#E1E4E8;">(</span><span style="color:#9ECBFF;">&quot;Hello, World!&quot;</span><span style="color:#E1E4E8;">)</span></span>\n<span class="line"><span style="color:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><br> For compile Jule code, we just give current directory path to JuleC:</p><div class="language- line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki github-dark"><code><span class="line"><span style="color:#e1e4e8;">julec .</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>And compiler is transpile our Jule code to C++ code. In compile mode, JuleC will show you the build command itself on the command line, which is used during compilation. You can also set the mod to transpile and choose to compile manually, or you can write a script that compiles the code after you have it in C++ form using the transpile mode to experience compile mode.</p><p>As result we have a executable machine code result of our program.</p><h2 id="backend-compiler-optimizations" tabindex="-1">Backend Compiler Optimizations <a class="header-anchor" href="#backend-compiler-optimizations" aria-label="Permalink to &quot;Backend Compiler Optimizations&quot;">​</a></h2><p>By default, JuleC sets your backend compiler&#39;s optimizations to the lowest possible level and compiles your code as such. These optimizations are independent of the compiler optimizations that JuleC has, they are the backend-compiler optimizations you use for IR compilation. The created executable file is available for debugging. The lowest optimization level usually allows the backend-compiler to exhibit the fastest compilation performance. But without the generated executable optimizations it can be significantly underperforming.</p><p>If you want to compile your code with backend-compiler optimizations, there are several ways you can do it.</p><h3 id="production-compilation" tabindex="-1">Production Compilation <a class="header-anchor" href="#production-compilation" aria-label="Permalink to &quot;Production Compilation&quot;">​</a></h3><p>Enable production compilation. This is the widely recommended and safest way. Your compiler compiles the code for you by applying the best standard optimizations according to the backend compiler you use. To get more detailed information about what improvements production compilation makes, you can look at the <a href="/compiler/compiler-optimizations.html#production-compilation">compiler optimizations</a> section.</p><h3 id="set-backend-compiler-optimizations-via-pass-directive" tabindex="-1">Set Backend Compiler Optimizations via <code>pass</code> Directive <a class="header-anchor" href="#set-backend-compiler-optimizations-via-pass-directive" aria-label="Permalink to &quot;Set Backend Compiler Optimizations via `pass` Directive&quot;">​</a></h3><p>You can add the <code>pass</code> directive to a suitable place in the main package of your program and adjust the optimizations of your backend-compiler. This method works most of the time.</p><p>For example to enable Clang&#39;s <code>O3</code> optimizations:</p><div class="language-jule line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">jule</span><pre class="shiki github-dark"><code><span class="line"><span style="color:#79B8FF;">#pass</span><span style="color:#E1E4E8;"> </span><span style="color:#9ECBFF;">&quot;-O3&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><h3 id="transpile-and-compile-manually" tabindex="-1">Transpile and Compile Manually <a class="header-anchor" href="#transpile-and-compile-manually" aria-label="Permalink to &quot;Transpile and Compile Manually&quot;">​</a></h3><p>You can transpile your code and compile your IR code with your desired optimization setting by customizing the suggested compile command by compiler or with a completely custom compile command.</p><h2 id="transpilation" tabindex="-1">Transpilation <a class="header-anchor" href="#transpilation" aria-label="Permalink to &quot;Transpilation&quot;">​</a></h2><p>Preferably, instead of compiling your code directly, you may want to transpile your code for various reasons to obtain the IR code. This scenario is mostly used when you want to distribute IR code, modify it, debug compiler&#39;s code generation, compile with different compilers or compilation commands.</p><p>To do this, pass the flag that you activate the transpile mode in your compiler. <br> For example:</p><div class="language- line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki github-dark"><code><span class="line"><span style="color:#e1e4e8;">julec --transpile .</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><h2 id="why-julec-needs-directory-path-instead-file-path" tabindex="-1">Why JuleC Needs Directory Path Instead File Path? <a class="header-anchor" href="#why-julec-needs-directory-path-instead-file-path" aria-label="Permalink to &quot;Why JuleC Needs Directory Path Instead File Path?&quot;">​</a></h2><p>In Jule, each program is also a package. Jule source codes in the directory are accepted within the package. Accordingly, every directory is actually a potential Jule package. The directory of the program you will compile should be the main package of your program.</p><p>This also eliminates the need to link individual source codes to the compiler and significantly avoids the occurrence of long compiler commands. It makes it easy to understand which of the source codes are in the main program, the answer is simple: all the Jule source codes in the directory. Because of this approach, each Jule program is kept in a separate directory as a package, causing optimistic pressure on the project organization.</p>',26),n=[s];function l(r,c,p,d,m,u){return a(),o("div",null,n)}const b=e(t,[["render",l]]);export{y as __pageData,b as default};
